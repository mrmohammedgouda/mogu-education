# دليل ربط الدومين بموقع MOGU Edu - خطوة بخطوة للمبتدئين

## 📋 المتطلبات الأساسية

قبل البدء، تأكد من أن لديك:

1. ✅ **دومين خاص بك** (مثل: moguedu.ca أو moguedu.com)
2. ✅ **حساب على Cloudflare** (مجاني 100%)
3. ✅ **الموقع الحالي جاهز** (تم بناؤه بالفعل ✓)

---

## 🎯 الطريقة الأسهل: استخدام Cloudflare Pages (موصى بها للمبتدئين)

هذه الطريقة **مجانية بالكامل** ولا تحتاج خبرة برمجية!

### الخطوة 1️⃣: إنشاء حساب Cloudflare (إذا لم يكن لديك حساب)

1. اذهب إلى: https://dash.cloudflare.com/sign-up
2. سجل حساب جديد (مجاني)
3. أدخل بريدك الإلكتروني وكلمة مرور

---

### الخطوة 2️⃣: الحصول على Cloudflare API Token

#### طريقة 1: من خلال واجهة GenSpark (الأسهل)

1. في واجهة GenSpark، اذهب إلى تبويب **"Deploy"**
2. ابحث عن قسم **"Cloudflare API Key"**
3. اضغط على **"Get API Token from Cloudflare"**
4. سيفتح لك صفحة Cloudflare
5. اضغط **"Create Token"**
6. اختر قالب **"Edit Cloudflare Workers"**
7. انسخ الـ Token واحفظه (سنستخدمه لاحقاً)

#### طريقة 2: من Cloudflare مباشرة

1. سجل دخول إلى: https://dash.cloudflare.com
2. اضغط على أيقونة المستخدم (أعلى يمين)
3. اختر **"My Profile"**
4. من القائمة الجانبية اختر **"API Tokens"**
5. اضغط **"Create Token"**
6. اختر قالب **"Edit Cloudflare Workers"**
7. اضغط **"Continue to summary"**
8. اضغط **"Create Token"**
9. **انسخ الـ Token وخزنه في مكان آمن** (لن تستطيع رؤيته مرة أخرى)

---

### الخطوة 3️⃣: إضافة الدومين إلى Cloudflare

#### أ. إضافة الموقع (Site) إلى Cloudflare

1. من لوحة تحكم Cloudflare: https://dash.cloudflare.com
2. اضغط **"Add a Site"**
3. أدخل اسم الدومين (مثال: moguedu.ca)
4. اختر الخطة **"Free"** (مجانية)
5. اضغط **"Continue"**

#### ب. تغيير Nameservers عند مزود الدومين

Cloudflare سيعطيك nameservers مثل:
```
adrian.ns.cloudflare.com
jade.ns.cloudflare.com
```

**الآن اذهب إلى مزود الدومين الخاص بك** (حيث اشتريت الدومين):

**مثال: إذا كان الدومين من GoDaddy:**
1. سجل دخول إلى GoDaddy
2. اذهب إلى **"My Products"**
3. اختر الدومين الخاص بك
4. اضغط **"Manage DNS"**
5. ابحث عن **"Nameservers"**
6. اضغط **"Change"**
7. اختر **"Custom"**
8. احذف الـ Nameservers القديمة
9. أضف الـ Nameservers من Cloudflare
10. احفظ التغييرات

**مثال: إذا كان الدومين من Namecheap:**
1. سجل دخول إلى Namecheap
2. اذهب إلى **"Domain List"**
3. اضغط **"Manage"** بجانب الدومين
4. ابحث عن **"Nameservers"**
5. اختر **"Custom DNS"**
6. أدخل الـ Nameservers من Cloudflare
7. احفظ

⏰ **ملاحظة مهمة**: قد يستغرق تحديث الـ Nameservers من 2-48 ساعة

---

### الخطوة 4️⃣: نشر الموقع على Cloudflare Pages

#### أ. تجهيز أدوات النشر

في واجهة GenSpark (الشات الحالي)، اكتب:

```
setup_cloudflare_api_key
```

سيطلب منك الـ API Token الذي حصلت عليه في الخطوة 2. الصقه.

#### ب. إنشاء قاعدة البيانات على Cloudflare

في واجهة GenSpark، اكتب الأوامر التالية واحداً تلو الآخر:

**1. إنشاء قاعدة بيانات:**
```bash
cd /home/user/webapp
npx wrangler d1 create moguedu-production
```

ستحصل على رد مثل:
```
database_id = "abc123-def456-ghi789"
```

**انسخ الـ database_id**

**2. تحديث ملف الإعدادات:**

اطلب من GenSpark تحديث `wrangler.jsonc` بالـ database_id:

```
قم بتحديث wrangler.jsonc واستبدل database_id بالقيمة: abc123-def456-ghi789
```

**3. إنشاء جداول قاعدة البيانات:**
```bash
cd /home/user/webapp
npx wrangler d1 migrations apply moguedu-production
```

**4. إضافة البيانات التجريبية (اختياري):**
```bash
cd /home/user/webapp
npx wrangler d1 execute moguedu-production --file=./seed.sql
```

#### ج. نشر الموقع

**1. بناء الموقع:**
```bash
cd /home/user/webapp
npm run build
```

**2. إنشاء مشروع Cloudflare Pages:**
```bash
npx wrangler pages project create moguedu --production-branch main
```

**3. نشر الملفات:**
```bash
npx wrangler pages deploy dist --project-name moguedu
```

✅ **مبروك!** موقعك الآن على الإنترنت على رابط مثل:
```
https://moguedu.pages.dev
```

---

### الخطوة 5️⃣: ربط الدومين الخاص بك

#### أ. إضافة الدومين إلى Cloudflare Pages

**في واجهة Cloudflare:**

1. اذهب إلى: https://dash.cloudflare.com
2. من القائمة الجانبية اختر **"Workers & Pages"**
3. اضغط على مشروع **"moguedu"**
4. اذهب إلى تبويب **"Custom domains"**
5. اضغط **"Set up a custom domain"**
6. أدخل اسم الدومين الكامل (مثال: moguedu.ca أو www.moguedu.ca)
7. اضغط **"Continue"**
8. اختر **"Activate domain"**

**أو باستخدام الأوامر في GenSpark:**

```bash
npx wrangler pages domain add moguedu.ca --project-name moguedu
```

#### ب. التحقق من الإعدادات

بعد إضافة الدومين، Cloudflare سيضبط كل شيء تلقائياً:

✅ سيتم إنشاء CNAME records تلقائياً
✅ سيتم تفعيل SSL/HTTPS تلقائياً
✅ سيعمل الدومين خلال دقائق

---

## 🎉 النتيجة النهائية

بعد اكتمال الخطوات، موقعك سيكون متاح على:

1. ✅ **الدومين الخاص بك**: https://moguedu.ca
2. ✅ **Cloudflare Pages URL**: https://moguedu.pages.dev
3. ✅ **SSL مفعل تلقائياً** (أمن 100%)
4. ✅ **سرعة عالية** (خوادم عالمية)

---

## 🔧 الطريقة البديلة: إذا كان لديك استضافة خاصة

إذا كنت تريد استخدام استضافة تقليدية (مثل cPanel):

### الخطوات:

1. **رفع الملفات إلى الاستضافة:**
   - احصل على ملفات الموقع من `/home/user/webapp/dist/`
   - ارفعها إلى مجلد `public_html` في الاستضافة

2. **إعداد قاعدة البيانات:**
   - ⚠️ **تحذير**: الموقع مصمم لـ Cloudflare D1 وليس MySQL
   - ستحتاج لتعديلات برمجية كبيرة
   - **غير موصى به للمبتدئين**

---

## ❓ أسئلة شائعة

### س1: كم تكلفة Cloudflare Pages؟
**ج:** مجاني تماماً! ✅

### س2: هل أحتاج خبرة برمجية؟
**ج:** لا! فقط اتبع الخطوات المذكورة. ✅

### س3: كم يستغرق تفعيل الدومين؟
**ج:** 
- تغيير Nameservers: 2-48 ساعة
- ربط الدومين بـ Cloudflare Pages: دقائق معدودة

### س4: هل يمكنني استخدام www.moguedu.ca و moguedu.ca معاً؟
**ج:** نعم! أضف كلاهما في Custom Domains. ✅

### س5: ماذا لو نسيت الـ API Token؟
**ج:** يمكنك إنشاء token جديد من Cloudflare. ✅

### س6: هل يمكن تغيير الدومين لاحقاً؟
**ج:** نعم! يمكنك إضافة أو إزالة دومينات في أي وقت. ✅

---

## 📞 هل تحتاج مساعدة؟

إذا واجهت أي مشكلة في أي خطوة، فقط أخبرني بالخطوة التي توقفت عندها وسأساعدك!

**يمكنك أن تقول:**
- "أنا عند الخطوة 2 ولم أفهم كيف أحصل على API Token"
- "نجحت في الخطوة 3 لكن لا أعرف كيف أغير Nameservers"
- "الموقع تم نشره لكن الدومين لا يعمل"

**سأساعدك خطوة بخطوة حتى يعمل موقعك على الدومين الخاص بك!** 🚀

---

## ✅ ملخص سريع (للمراجعة)

1. 📝 احصل على Cloudflare API Token
2. 🌐 أضف الدومين إلى Cloudflare
3. 🔧 غير Nameservers عند مزود الدومين
4. ⏰ انتظر 2-48 ساعة لتحديث الـ DNS
5. 🚀 انشر الموقع على Cloudflare Pages
6. 🔗 اربط الدومين بـ Cloudflare Pages
7. 🎉 استمتع بموقعك على الدومين الخاص!

---

**تم إعداد هذا الدليل خصيصاً لموقع MOGU Edu ✨**
